use crate::import;

pub fn load_and_run(
    request_path: &std::path::Path,
    env_path: Option<&std::path::Path>,
    private_env_path: Option<&std::path::Path>,
) {
}

#[cfg(test)]
mod tests {
    #[test]
    pub fn test_simple_requests() {}
}
